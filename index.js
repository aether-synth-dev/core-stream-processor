// File: index.js
/**
 * Core Stream Processor
 * 
 * A production-ready Node.js module that processes data streams with integrated
 * telemetry and sanitization capabilities following Clean Architecture principles.
 * 
 * @module core-stream-processor
 */

const Processor = require('./lib/processor');

module.exports = Processor;
